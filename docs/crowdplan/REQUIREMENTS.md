# Requirement traceability and evidence ledger

Status starts **Not started**. This package is specification material, not implementation evidence. Each CP ID maps to PRODUCT-SPEC.md; every original section is also preserved in MASTER-SPEC.md. A requirement spanning phases is not complete until all its behavior is verified. Conditional outcomes must be documented rather than silently waived.

Evidence key: **unit** = `tests/unit/*` (vitest, pure logic); **int** = `tests/integration/*` against the `crowdplan-test` Supabase project; **e2e** = `e2e/*` Playwright, multi-user, real Supabase auth/RLS/realtime (test project, local server); **prod** = the same Playwright suite against the production URL. Fixture-labeled provider data never counts as live-integration evidence.

| ID | Requirement | Phases | Acceptance summary | Status / evidence |
| --- | --- | --- | --- | --- |
| CP-01 | Product purpose and scope | 00 | Capture settled/flexible dimensions, collect constraints, evaluate feasibility and compromises, and reach consensus; recommendations are optional. | Verified (test env) — scenarios A–I in e2e |
| CP-02 | Dimension model | 02 | LOCKED, CONSTRAINED, and UNDECIDED are enforced by the engine; context-specific dimensions are supported. | Verified (test env) — unit `engine.test.ts`; e2e A (board stamps), D (clarified rule becomes CONSTRAINED) |
| CP-03 | Natural-language creation | 03 | Simple prompt, editable interpretation, visible states, and confirmation of extracted values. | Verified (test env) — e2e A/B/C/H create via prompt → "Here's what I understood" → edit/confirm |
| CP-04 | Interpretation and ambiguity | 02,03 | Provider abstraction, Anthropic preference, validated output, deterministic fallback, and clarification before hard constraints. | Verified (fallback path) — unit `interpretation.test.ts`, e2e D (Sunday-style clarification before activation). Anthropic adapter implemented + schema-validated; **live Anthropic not exercised** (no `ANTHROPIC_API_KEY`, optional per spec) |
| CP-05 | Organizer authentication | 01 | Supabase Auth; email link/OTP sufficient; Google OAuth when configured. | Verified (test env) — int `auth-rls.test.ts` (password + token-hash), e2e organizer sign-in through `/login` + `/auth/confirm`. Google OAuth not configured (optional, hidden) |
| CP-06 | Guest participation and access | 01,03 | Secure plan invitation, display name, no account requirement, plan-scoped RLS and optional account claim. | Verified (test env) — int isolation/revocation tests; e2e A (four guests join without accounts); int `account.test.ts` (optional single-use account claim) |
| CP-07 | Contextual response flow | 03 | Activity, dinner, and travel questions; separate hard requirements from soft preferences. | Verified (test env) — e2e A (activity), B (dinner budget/preferences), H (travel origin/budget), F (hard rule) |
| CP-08 | Availability | 02,03 | IDEAL/WORKS/NOT AVAILABLE, date and time ranges, hard exclusions, overlap and participant views. | Verified (test env) — unit `availability.test.ts`; e2e A (strongest overlap correct), mobile painter |
| CP-09 | Planning modes | 03,04 | Fixed option, criteria-based, optional discovery, and shortlist; explicit opt-in for expanding candidates. | Verified (test env) — e2e A (fixed place, no alternatives), B (criteria search waits for availability), C (exactly the named shortlist, no broad search); discovery opt-in in demo + unit |
| CP-10 | SerpAPI providers | 04,06 | Local/maps, search, flights, hotels; server-only normalized provider interfaces. | **Blocked (live)** — normalizers/cache/budget verified in unit `providers.test.ts` with labeled fixtures; live calls need `SERPAPI_API_KEY` (missing) |
| CP-11 | Data trust | 04,06 | Provider/time/source attribution; recent/live, estimate, user-entered, unknown and visible demo labels. | Verified (test env) — e2e H (source + timestamp labels), demo banner on `/demo/*`; live-provider labels pending CP-10 |
| CP-12 | API cost controls | 04 | Normalized cache keys, TTL, deduplication, counters, per-plan budget, reuse, shortlist refresh, bounded search. | Verified (logic) — unit `providers.test.ts` (canonical keys, TTL, claim dedup, budget reserve, bounded retries); live spend pending CP-10 |
| CP-13 | Universal candidates | 02,04,06 | Normalized places/events/custom/travel candidates and component candidates with provenance. | Verified (test env) — unit travel totals; e2e C (custom), H (travel components) |
| CP-14 | Hard feasibility | 02,05 | Deterministic participant predicates and explicit violations; universal failure cannot rank as feasible. | Verified (test env) — unit `engine.test.ts`; e2e F (person + exact reason) |
| CP-15 | Soft scores and fairness | 02,05 | Average/minimum satisfaction, hard/soft compromises, ideal count, budget and convenience distribution. | Verified (test env) — unit fairness; e2e B (ranking + fairness visible) |
| CP-16 | Structured explanations | 02,05 | Positives, compromises and hardViolations are generated from engine facts, never invented by an LLM. | Verified (test env) — unit explanation objects; e2e B/F |
| CP-17 | Consensus and objections | 05 | Five reactions; reason for CANNOT do; confirm new hard constraints; honest alignment state. | Verified (test env) — unit consensus; e2e E (reaction realtime), F/G (objection → confirmed rule) |
| CP-18 | Make This Work | 06 | Identify blockers, seek bounded minimal changes, show individual impacts, re-evaluate and ask focused questions. | Verified (test env) — unit repair; e2e G (8:30 proposal; no-fix for price clash) |
| CP-19 | Resolution board | 03,05,08 | Settled/unresolved dimensions, participants, budget and consensus; LOCKED/CONSTRAINED/UNDECIDED/RESOLVED/BLOCKED. | Verified (test env) — e2e A/D board states |
| CP-20 | Realtime | 05 | Votes, response counts, rankings and consensus update without refresh through Supabase Realtime. | Verified (test env) — int member-only realtime; e2e E (organizer sees guest reaction without refresh) |
| CP-21 | Database | 01 | Normalized Supabase PostgreSQL, migrations, organizer/guest permissions and RLS. | Verified — migrations 0100–0900 on test and production (identical catalog); int `auth-rls.test.ts` 30/30 suite passes on both |
| CP-22 | Maps | 04,08 | Use MapLibre when practical and location answers a planning question; no decorative maps. | Verified (browser) — options map + final-plan map on `/demo/shortlist`, zero console errors after worker fix |
| CP-23 | Notifications | 07 | In-app notifications; email through Resend when configured; avoid duplicate/excessive messages. | Verified in-app — int `jobs.test.ts` (notification rows, dedupe keys, reminders only to missing members). Email conditional — `RESEND_API_KEY` not configured |
| CP-24 | Automation and deadlines | 07 | Adaptive scheduled refresh/recalculation/reminders/deadline/consensus updates; minimal finalized-plan refresh. | Verified (test env) — int `jobs.test.ts` (reminders only to missing members, idempotent deadline/consensus, cadence); pg_cron dispatch configured |
| CP-25 | Finalization | 07 | Organizer finalizes, all guests see final activity/travel details; booking links only. | Verified (test env) — e2e I (every guest sees same final plan; no-booking note) |
| CP-26 | Consumer design and visualizations | 08 | Premium responsive mobile/desktop UX, six useful signature views, thoughtful states and meaningful motion. | Verified (test env) — desktop 1440 + mobile 390 e2e; board, heatmap, timeline, options/fairness, map, final plan |
| CP-27 | PWA and themes | 08 | Installable PWA and excellent light/dark modes if practical; record the outcome. | Implemented — manifest, icons, service worker (prod only), offline page, light/dark toggle; install check pending prod |
| CP-28 | Saved preferences | 07 | Optional profile claim and stable preference prefill, editable and confirmed when sensitive or uncertain. | Verified (test env) — int `account.test.ts` (single-use claim ticket moves guest membership to an account; owner-only preferences; guests cannot store preferences); e2e `saved-preferences.spec.ts` (prefill offered, applied only on confirm, editable, not re-offered) |
| CP-29 | Preferred stack and secrets | 00,01 | Inspect environment, prefer requested stack, use current docs, never hardcode or leak secrets. | Verified — requested stack; secrets only in env/app_settings hashes; staged-diff secret scan before each push |
| CP-30 | Demo plans | 08 | Clearly labeled Vala’s, three-restaurant shortlist, Saturday discovery, and Vegas October plans. | Verified (browser) — `/demo/{valas,shortlist,saturday,vegas}` with persistent demo banner, not stored as live records |
| CP-31 | Automated tests | 02,09 | All specified domain areas tested; meaningful failing-case assertions and release checks pass. | Verified — 80 unit, 30 integration (test + production Supabase), 13 Playwright (test env + local production build on production Supabase), 2026-09-22 |
| CP-32 | Playwright A–I | 09,10 | Realistic multi-user browser scenarios, production authentication, integrations, mobile and desktop. | In progress — A–I (+mobile, +preferences) 13/13 on test env and 13/13 on a local production build against production Supabase; run on the Vercel URL **blocked** (Vercel authorization); live-provider portions of B/H blocked by CP-10 |
| CP-33 | Production security | 01,04,09 | Keys, RLS, guest isolation, unsafe URLs/HTML, rate limits, schema validation and SSRF protection. | Verified (test env) — int isolation; unit `urls.test.ts` (SSRF); CSP/HSTS/frame headers; shared rate limits; zod on every action |
| CP-34 | Performance | 04,08,09 | Fast initial load, cached expensive data, bounded rendering, useful progress and appropriate optimism. | In progress — provider cache + bounded search verified; production timing pending phase 10 |
| CP-35 | Git and deployment | 00,10 | Coherent commits, authenticated GitHub push when available, Vercel production, migrations and env. | In progress — commits pushed to origin/main; production Supabase migrated (0100–0900) and verified; Vercel deploy **blocked** (403, re-authentication required) |
| CP-36 | Autonomy and completion | 00,10 | Continue through verified production, fix critical bugs, provide live URL; no silent core scope cuts. | In progress — blocked only by Vercel authorization and `SERPAPI_API_KEY`; see PROGRESS.md |

## Evidence rules

Replace status with In progress, Blocked, Implemented/unverified, or Verified. Attach repository-relative test/report paths and the verified commit/environment. Distinguish automated logic tests, fixture-based browser tests, live provider checks and production checks. Mark conditional optional features with a documented decision and reason, never use that status for a core requirement.
